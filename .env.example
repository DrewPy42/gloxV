MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=glox
MYSQL_USER=gloxv
MYSQL_PASSWORD=

VITE_PORT=5174

# URL/port the client uses to reach the API server (host-side, not the
# container-internal port). Adjust SERVER_PORT per machine if it conflicts
# with something else running locally.
SERVER_URL=http://localhost
SERVER_PORT=3001
